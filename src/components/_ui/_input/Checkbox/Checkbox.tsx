import React from 'react';


export type CheckboxProps = {
    onChange: (value: boolean) => any;
    label: string;
}

const Checkbox = () => {
    return (
        <div>
            Checkbox
        </div>
    );
};

export default Checkbox;